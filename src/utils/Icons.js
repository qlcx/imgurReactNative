import React from 'react'
import Icons from 'react-native-vector-icons/FontAwesome'

const tabBarBottomIcon = iconName => {
  return <Icons name={iconName} size={20} color='#fff' />
}

export default tabBarBottomIcon